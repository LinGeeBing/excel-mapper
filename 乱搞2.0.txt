下面继续开发：
1.模版库的模版顺序也可以移动：跟字段定义的移动一样，唯一不同的是字段定义是通过拉动数字移动，模版库这里是拖动模版名称移动
2.模版库里“自定义模版”按钮的功能：点击跳出字段定义的弹窗，鼠标停留在“模版名称”的输入框中
请直接修改我的代码并给出修改后的完整代码，直接复制即可




后面把需要整理的表格称为原表
接下来要完成读取原表的功能，在页面中的“表格整理”区域实现功能
以下是一些需求，请先根据我的需求完善设计思路，待会再进行代码开发
前面提到的字段匹配库不是一成不变的，是会跟着更新的，具体形式后面会提到
首先在导入原表之前要先选好模板

原表的读取：有一个按钮“上传原表”，点击以后跳出弹窗，这个弹窗和“上传模板”的弹窗基本一样，改动的地方：“模板”全都改成“原表”两个字，上传方式包括拉取上传还有选取上传两种方式，
顺便完善一下一个功能，当上传文件以后，模版名称的输入框自动填入该文件的名称
导入的文件格式包括.csv/.xlsx/.xls，同样有预览的功能，但是这个预览是能够看到一整个文件内容的，这个预览的弹窗可以大很多很多。
导入原表之后点击“确定”
会弹出一个“字段匹配”的弹窗，这个弹窗有三列属性，“序号”、“模板字段”、“原表字段”三列，其中“模板字段”来源于上面选择好的模板，必选的字段前面有一个红色的“※”符号，“原表字段”是一列输入框，会先用原表字段和模板字段对应的匹配库里的内容进行比较，完全一样的可以直接填入原表字段。还有一种情况，就是在匹配库里找不到，这个时候可以双击输入框，有一个多选框可以选择原表字段，选择之后还可以添加一些符号，比如“原表字段1”&“原表字段2”同时作为“模板字段1”的内容。（可以理解我的意思吗，这个要求可以实现吗）原表字段出现在输入框里是有颜色的字体或者是有一个有颜色的圆角矩形框圈起来，可以跟普通字符进行区分，比如同个表格里面出现的一个字段可能是完全相同的，例如销售渠道，全都是“淘宝”，那么这个匹配字段里就是“淘宝”两个汉字，后面生成的文件中，这一列都是这两个汉字，这种没有涉及到原表字段的匹配记录就不用记录到匹配库里
后面生成表格以后这个匹配的记录就被记录在匹配库里，（匹配库是有学习性的，不是一成不变的，要实时更新）
完成以后点击右下角的确定，这个时候要检查必填字段是否已经匹配，不可为空，
这个弹窗的左下角有一个“查看原表”的按钮，点击和前面预览的功能一样（方便查看各个原表字段的内容）



有一些地方需要修改
1.要求上传原表的时候也要和上面上传模板文件一样上传之后可以预览
2.点击上传原表按钮弹出弹窗之前要检查是否已经选择好模版，必须要先选好模版才可以上传原表，而不是上传完原表才做判断
3.※表示必填字段是直接展示的，比如“字段1”是必填字段，那么它就在字段匹配的弹窗中前面自带同一个红色的※
4.字段匹配的弹窗左上角不要写字段弹窗，而是写对应的原表名称
5.原表字段选择是一个输入框的形式，输入框后面有一个加号，点击加号可以选择原表字段，也可以自己在输入框里输入内容



有以下内容需要修改，先不要开发，先清楚我的思路
1. 必填字段的展示还是不对，比如“字段1”是必填项，那么在匹配弹窗就应该展示的是“※字段1”这样子
2. 原表字段双击可以修改，没有双击之前不要显现输入框，还有选择原表的字段之后那个字段要和其他的字不一样

下面是一些新的功能：
表格整理这个板块下面有一个表格，这个表格是用来记录原表模板化之后的表格
要注意的是，这个表格是对应上面的模版的。每次要上传原表之前都要先选择模版，然后才能传入原表，进行模板化。只有用同一个模板处理的原表才能合并，所以下面的表格记录的原表对应的是同个模板的，要在上面先选好模版，才能展示出对应的表格。
这个表格有几列，分别是：序号；“原表”（这一列写的是原表的名称）；“操作时间”；
每一行后面有一个“预览”按钮（点击可以看到被模板化处理后的原表），还有一个“导出”的按钮，点击可以将这个模板化的原表文件导出（就是平时下载文件那样，可以指定路径保存之类的）
每一行的最前面有一个多选框，“上传原表”的按钮后面添加一个“合并表格”的按钮，这些一行一行的记录可以被多选，之后点击这个按钮可以被合并到一起，公用一个模版表头，内容合并，生成一个新的文件，对这个新的文件进行命名之后会添加在这个记录的表格最下面。

字段匹配完，点击确定的时候，表格整理的板块下面要生成一行这个原表的内容匹配进模板的记录
每添加一个原表就会对应生成一行记录




有以下问题需要修改，请先不要开发，先清楚我的思路
1.为什么我编辑完模板的必填字段之后没有保存下来这个记录
2.有个bug，就是我还没上传原表的时候点击字段匹配也能够进行下一步，应该要选完原表才能进行下一步
3.双击选择原表字段的时候，这个选择的框可以占满一整个单元格比较好选，把双击编辑改成单击，还有为什么点击后面的加号这个框框就失焦了没有办法选择
4.添加完的表格应该要可以再进行修改，所以在每一行数据后面除了有预览和导出，还应该有“编辑”功能，点击编辑就弹出匹配字段的弹窗



为什么有弹窗出现的时候，左边的菜单栏会往右移动一些
导出的问题依然存在，一点击下载就有导出成功的提示出现
表格整理的文件点击编辑的时候之前的内容全部清空了，不应该保留之前编辑的痕迹吗
还有就是在进行字段匹配的时候，输入的内容会用来搜索而不是被读取，比如原表里没有渠道这个字段，我想要这个表格里的这个字段全都填上“淘宝”，这个时候我可以直接输入“淘宝”两个字，这是我想要的效果